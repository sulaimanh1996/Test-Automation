# UV

uv init = start
uv add = install package
uv remove = verwijder
uv venv = virtual environment
als er () dan in virtuele environment
.\.venv\Scripts\Activate.ps1 activate environemnt
deactivate turn off
uv sync syncs everything from pyproject.toml
